{
  "id": "sql-to-types",
  "label": "PostgreSQL CREATE TABLE: a column without NOT NULL accepts NULL",
  "steps": [
    { "action": "select", "field": "dialect", "value": "postgresql" },
    { "action": "select", "field": "target", "value": "typescript" },
    {
      "action": "fill",
      "field": "input",
      "value": "CREATE TABLE users (id integer PRIMARY KEY, email text NOT NULL, bio text);"
    }
  ],
  "expect": ["email: string;", "bio: string | null;"]
}
