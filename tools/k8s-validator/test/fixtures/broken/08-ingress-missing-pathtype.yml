# Broken: an Ingress path with no pathType (required since networking.k8s.io/v1,
# per the deprecated API migration guide's own Ingress notes: "pathType is now
# required for each specified path").
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress
spec:
  rules:
    - http:
        paths:
          - path: /testpath
            backend:
              service:
                name: test
                port:
                  number: 80
