import type { EmitResult } from './emit-typescript';
import type { InferredModel } from './infer';

// RED stub for Task 2's TDD cycle: implemented for real in the GREEN commit.
export function emitKotlin(_model: InferredModel): EmitResult {
  return { output: '', warnings: [] };
}
