# Broken: "step" is not a recognised job key (the correct key is "steps").
# https://docs.github.com/en/actions/reference/workflow-syntax-for-github-actions
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    step:
      - run: echo hi
